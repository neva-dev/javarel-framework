apply plugin: 'groovy'
apply plugin: 'maven'

sourceCompatibility = 1.7

group = 'com.neva.osgi'
version = '1.0.0'

dependencies {
    compile gradleApi()
    compile localGroovy()

    compile group: 'biz.aQute.bnd', name: 'biz.aQute.bndlib', version: '3.1.0'
}

repositories {
    jcenter()
}