buildscript {
    apply from: rootProject.file('buildscript.gradle'), to: buildscript
}

description = 'Javarel - Administrative Application'

apply plugin: 'kotlin'
apply plugin: 'osgi-module'
apply plugin: 'com.athaydes.ipojo'

jar {
    manifest {
        instruction 'Bundle-Name', project.description
        instruction 'Private-Package', "com.neva.javarel.app.adm.api.*;version=${project.version}"
    }
}

dependencies {
    compile project(':app:core')
}